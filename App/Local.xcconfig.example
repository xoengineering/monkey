// Copy this file to Local.xcconfig (gitignored, never committed) and fill in
// your own bundle identifier. script/bootstrap does this for you
// automatically if Local.xcconfig doesn't exist yet.
//
// Signing team is deliberately not set here: with CODE_SIGN_STYLE Automatic
// and no team configured, Xcode's Signing & Capabilities tab will prompt you
// to pick one, and that choice is stored per-user in gitignored xcuserdata —
// never in the shared, tracked project file. For a one-off command-line
// build, pass it as a trailing build setting instead:
// `xcodebuild ... DEVELOPMENT_TEAM=YOURTEAMID build`

PRODUCT_BUNDLE_IDENTIFIER = com.yourdomain.Monkey

// The app group ID below is a literal string baked into Monkey.entitlements
// and MonkeyCLI.entitlements (entitlements don't support build-setting
// substitution the way Info.plist/xcconfig do). If you change
// PRODUCT_BUNDLE_IDENTIFIER above, update "group.com.yourdomain.Monkey" in
// both entitlements files to match, and register that App Group ID under
// your own team in the Apple Developer portal before building.
